Retrieve terraform code for a google compute network and a google compute subnetwork resources.

The google compute network is named vpc and has disabled the flag "auto_create_subnetworks".

The google compute subnetworks is named subnetwork, is on the region "us-central1", has ip cidr range set to "10.2.0.0/16" and its network is linked to the vpc id.