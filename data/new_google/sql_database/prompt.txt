Provide terraform code containing the following resources:
- A google_sql_database_instance, having the name set to "instance" in the region "us-central1" and the database version of "POSTGRES_15", for the settings' block: the tier is "db-f1-micro".
- A google_sql_database of name "database" and having the attribute "instance" linked to "instance"'s name.